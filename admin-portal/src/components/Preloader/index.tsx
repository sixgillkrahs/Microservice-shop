import Loading from "../Loading";
import "./index.css";

const Preloader: React.FC = () => {
  return (
    <div className="container">
      <Loading />
    </div>
  );
};

export default Preloader;
